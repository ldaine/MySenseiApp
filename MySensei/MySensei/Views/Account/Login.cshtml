@model MySensei.Models.LoginModel
@{
    Layout = "~/Views/Shared/_LoginFront.cshtml";
}
@{ ViewBag.Title = "Login";}
<h2>Log In</h2>
@Html.ValidationSummary()
@using (Html.BeginForm())
{
    @Html.AntiForgeryToken();
    <input type="hidden" name="returnUrl" value="@ViewBag.returnUrl" />
    <div class="form-group">
        <label>Name</label>
        @Html.TextBoxFor(x => x.UserName, new { @class = "form-control" })
    </div>
    <div class="form-group">
        <label>Password</label>
        @Html.PasswordFor(x => x.Password, new { @class = "form-control" })
    </div>
    <button class="btn btn-primary" type="submit">Log In</button>
    
}
<p>
    @Html.ActionLink("Register as a Student",
                     "Register",
                     new { role = "student" })
</p>
<p>
    @Html.ActionLink("Register as a Teacher",
                     "Register",
                     new { role = "teacher" })
</p>
<p><a href="#">Continue as guest</a></p>
    <nav>
        <ul>
            <li><img src=" ~/img/logo_i_vector_sort.png" alt="MySensei"/></li>        
            <li><a href="#">Link</a></li>
            <li><a href="#">Link</a></li>
            <li><a href="#">Link</a></li>
            <li><a href="#">Link</a></li>
        </ul>
    </nav>

@section price {
    <div style="width: 100%; height: 100vh; border: 1px solid black; margin-top: 100vh;">
        <div>@Html.ActionLink("Music", "FrontCategories", new { ids = 1 })</div>
        <div>@Html.ActionLink("Sport", "FrontCategories", new { ids = 2 })</div>
        <div>@Html.ActionLink("Art", "FrontCategories", new { ids = 3 })</div>
        <div>@Html.ActionLink("School", "FrontCategories", new { ids = 4 })</div>
        @*@foreach (var stuff in ViewBag.Appcourses)
        {

            <div style="width: 300px; height: 150px; display: inline-block; border: 1px solid black; padding: 5px; margin: 5px; color: black;">
                <p>Headline: @stuff.Headline </p>
                <p>Description: @stuff.Description </p>
            </div>
        }*@
    </div>
}